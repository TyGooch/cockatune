json.extract! playlist, :id, :name, :user_id, :playlist_songs, :playlist_image_url

# json.playlist do
#   json.id @playlist.id
#   josn.name @playlist.name
#
#   json.songs @playlist.playlist_songs.sort_by{|p1, p2|}
# end
